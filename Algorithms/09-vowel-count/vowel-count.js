// Write code to return the the number of vowels in `str`

var vowelCount = function(str) {
    let el = [b, e, f, g, c, d];
    for(let i = 0; i < arr.length; i++) {
        let num = 0;
        if(str.toLowerCase().includes(el[i])) {
            num++;
        }
    }
};
